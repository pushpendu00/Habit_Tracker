<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Habit Tracker app</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <header>
        <span id="logo">Habit Tracker</span>        
        <span id="username">Pushpendu</span>        <!--ejs -->
    </header>

    <main>
        <br>
        <form action="/add-habits" method="post" id="input-form">
            <input type="text" name="habit" placeholder="Enter your habits for adding....." id="input-habits" required>
            <!-- <input type="submit" value="Add habits" > -->
            <button type="submit" id="add-button">Add habits</button>
        </form>
        <div id="habits-list">
                <ul>
                    <%if(habits.length<=0){%>
                        <h2>Your habits are empty.....</h2>
                    <%}%>
                    <%for(let i=0;i<habits.length;i++){%>
                        <li class="habits-content">
                            <div class="list" id="<%= habits[i]._id %>">
                                <div class="list-div1">
                                    <div class="habits">
                                        
                                            <input type="checkbox" name="perform" onchange="changeStatus(event)" <% if(habits[i].oneweek[habits[i].oneweek.length-1].status == true){ %> checked <% } %> >
                                    
                                    </div>
                                </div>
                                <div class="list-div2">
                                    <div class="list-div2-habitName-and-days">
                                        <h2><%= habits[i].habitName %></h2>
                                        <h4><%= habits[i].workDay %>/<%= habits[i].totalDay %> Days</h4>
                                    </div>
                                    <div class="list-div2-time-views-delete">
                                        <div>
                                            <!-- inserted date -->
                                            <%= habits[i].addDate %>    
                                        </div>
                                        <br>
                                        <!-- <div class="delete-and-views-div"> -->
                                            <button type="submit" class="weekly-views-button" onclick="showWeeklyViews(event)">Weakly views</button>
                                            <br>
                                            <button type="submit" class="delete-button" onclick="deleteHabits(event)">Delete</button>
                                        <!-- </div> -->
                                    </div>
                                </div>
                            </div>
                            <div class="weekly-views" id="views-<%= habits[i]._id %>">
                                <% for(let j=0;j<habits[i].oneweek.length;j++){ %>
                                    <div class="weekly-views-div">
                                        <%= habits[i].oneweek[j].date %>/<%= habits[i].oneweek[j].month %>
                                        <% if(habits[i].oneweek[j].status == true){ %>
                                            <img src="/image/done.jpg">
                                        <%} else{ %> 
                                            <img src="/image/not_done.jpg">
                                        <% } %>
                                    </div>
                                <%}%>
                            </div>
                        </li>
                    <%}%>
                </ul>
            
        </div>
    </main>

    <footer>
        &COPY;Pushpendu Jana
    </footer>

    <script src="/js/script.js"></script>

</body>
</html>